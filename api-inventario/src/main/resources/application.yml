server:
  port: 8083

spring:
  application:
    name: ms-inventario

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
      #Ubica donde se aloja eureka para mostrar el servicio